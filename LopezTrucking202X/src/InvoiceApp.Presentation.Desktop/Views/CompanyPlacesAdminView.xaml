<Window x:Class="InvoiceApp.Presentation.Desktop.Views.CompanyPlacesAdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Company Places" Height="600" Width="900">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Companies" FontWeight="SemiBold" Margin="0 0 0 8" />
            <ListBox ItemsSource="{Binding Companies}"
                     SelectedItem="{Binding SelectedCompany}"
                     DisplayMemberPath="Name" />
        </StackPanel>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 12">
                <ComboBox ItemsSource="{Binding Places}"
                          SelectedItem="{Binding SelectedPlaceToAdd}"
                          DisplayMemberPath="Name"
                          Width="240"
                          Margin="0 0 8 0" />
                <TextBox Text="{Binding SortOrderToAdd, UpdateSourceTrigger=PropertyChanged}"
                         Width="80"
                         Margin="0 0 8 0" />
                <Button Content="Add" Command="{Binding AddLinkCommand}" Width="80" />
            </StackPanel>

            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding CompanyPlaces}"
                      SelectedItem="{Binding SelectedLink}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Place" Binding="{Binding PlaceName}" Width="3*" IsReadOnly="True" />
                    <DataGridTextColumn Header="Sort Order"
                                        Binding="{Binding SortOrder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 12 0 0">
            <Button Content="Remove" Command="{Binding RemoveLinkCommand}" Width="90" Margin="0 0 8 0" />
            <Button Content="Save" Command="{Binding SaveCommand}" Width="90" Margin="0 0 8 0" />
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Width="90" Margin="0 0 8 0" />
            <Button Content="Close" Width="90" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
