<Window x:Class="InvoiceApp.Presentation.Desktop.Views.PlacesAdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Places" Height="600" Width="980">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding Places}"
                  SelectedItem="{Binding SelectedPlace}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="0 0 12 0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
                <DataGridTextColumn Header="City" Binding="{Binding City}" Width="*" />
                <DataGridTextColumn Header="State" Binding="{Binding State}" Width="*" />
                <DataGridCheckBoxColumn Header="Origin" Binding="{Binding IsOrigin}" Width="*" />
                <DataGridCheckBoxColumn Header="Destiny" Binding="{Binding IsDestiny}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Details" FontWeight="SemiBold" Margin="0 0 0 8" />

                <TextBlock Text="Name" />
                <TextBox Text="{Binding SelectedPlace.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0 4 0 8" />

                <TextBlock Text="Address Line 1" />
                <TextBox Text="{Binding SelectedPlace.AddressLine1, UpdateSourceTrigger=PropertyChanged}" Margin="0 4 0 8" />

                <TextBlock Text="Address Line 2" />
                <TextBox Text="{Binding SelectedPlace.AddressLine2, UpdateSourceTrigger=PropertyChanged}" Margin="0 4 0 8" />

                <TextBlock Text="City" />
                <TextBox Text="{Binding SelectedPlace.City, UpdateSourceTrigger=PropertyChanged}" Margin="0 4 0 8" />

                <TextBlock Text="State" />
                <TextBox Text="{Binding SelectedPlace.State, UpdateSourceTrigger=PropertyChanged}" Margin="0 4 0 8" />

                <TextBlock Text="Postal Code" />
                <TextBox Text="{Binding SelectedPlace.PostalCode, UpdateSourceTrigger=PropertyChanged}" Margin="0 4 0 8" />

                <CheckBox Content="Is Company" IsChecked="{Binding SelectedPlace.IsCompany}" Margin="0 4 0 4" />
                <CheckBox Content="Is Origin" IsChecked="{Binding SelectedPlace.IsOrigin}" Margin="0 4 0 4" />
                <CheckBox Content="Is Destiny" IsChecked="{Binding SelectedPlace.IsDestiny}" Margin="0 4 0 4" />
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 12 0 0">
            <Button Content="New" Command="{Binding NewPlaceCommand}" Width="90" Margin="0 0 8 0" />
            <Button Content="Save" Command="{Binding SavePlaceCommand}" Width="90" Margin="0 0 8 0" />
            <Button Content="Delete" Command="{Binding DeletePlaceCommand}" Width="90" Margin="0 0 8 0" />
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Width="90" Margin="0 0 8 0" />
            <Button Content="Close" Width="90" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
