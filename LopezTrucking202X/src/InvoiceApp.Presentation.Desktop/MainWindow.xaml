<Window x:Class="InvoiceApp.Presentation.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InvoiceApp" Height="450" Width="800">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="200" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <TextBlock Grid.Row="0" Grid.Column="0" Text="Amount Base" VerticalAlignment="Center" />
    <TextBox Grid.Row="0" Grid.Column="1"
             Text="{Binding AmountBase, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Grid.Row="1" Grid.Column="0" Text="Empties Count" VerticalAlignment="Center" Margin="0,8,0,0" />
    <TextBox Grid.Row="1" Grid.Column="1" Margin="0,8,0,0"
             Text="{Binding EmptiesCount, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Grid.Row="2" Grid.Column="0" Text="Empty Unit Price" VerticalAlignment="Center" Margin="0,8,0,0" />
    <TextBox Grid.Row="2" Grid.Column="1" Margin="0,8,0,0"
             IsReadOnly="True"
             Text="{Binding EmptyUnitPrice}" />

    <TextBlock Grid.Row="3" Grid.Column="0" Text="Line Total" VerticalAlignment="Center" Margin="0,8,0,0" />
    <TextBox Grid.Row="3" Grid.Column="1" Margin="0,8,0,0"
             IsReadOnly="True"
             Text="{Binding LineTotal}" />
  </Grid>
</Window>
